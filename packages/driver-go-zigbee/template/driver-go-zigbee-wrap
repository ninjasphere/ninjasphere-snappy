#!/bin/sh
app=$(readlink -f "$(pwd)")
version=$(basename "$app")
name=$(basename "$(dirname "$app")")
export SNAPP_APP=${SNAPP_APP:-$app}
export SNAPP_APP_DATA_PATH=${SNAPP_APP_DATA_PATH:-/var/lib/apps/${name}/${version}}
exec ./ninja-shim ./driver-go-zigbee "$@"
